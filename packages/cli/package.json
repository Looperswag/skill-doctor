{
  "name": "skill-doctor",
  "version": "0.1.0",
  "type": "module",
  "description": "Agent skill, hook, and subagent clinic with a bundled pixel treatment dashboard.",
  "bin": {
    "skill-doctor": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/index.js",
    "dist/index.d.ts",
    "dist/clinic",
    "templates",
    "fixtures",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/build-cli.mjs",
    "test": "vitest run src"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
