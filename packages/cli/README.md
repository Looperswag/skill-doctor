# skill-doctor CLI

The npm package bundles:

- CLI commands
- the prebuilt Pixel Clinic app
- the copyable `skill-doctor` skill template
- demo fixtures

Run:

```bash
npx skill-doctor@latest clinic --home
```
